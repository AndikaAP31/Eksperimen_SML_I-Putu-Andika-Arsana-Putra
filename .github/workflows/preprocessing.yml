name: Automated Data Preprocessing

on:
  # Trigger pada setiap push ke branch main (filter paths dihapus dulu untuk debug)
  push:
    branches:
      - main
  
  # Trigger manual
  workflow_dispatch:

# PENTING: Memberikan izin agar Action bisa nge-push balik ke repo
permissions:
  contents: write

jobs:
  preprocess-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
          cache: 'pip'
      
      - name: Install dependencies
        # Kita install manual library yang dipakai di script Anda
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy scikit-learn matplotlib seaborn
      
      - name: Verify file structure (Debug)
        run: |
          ls -R
      
      - name: Run preprocessing script
        # PENTING: Kita masuk ke folder preprocessing dulu
        # Agar logika '../stroke.csv' di script python Anda valid
        run: |
          cd preprocessing
          python automate_Andika.py
      
      - name: Check for changes
        id: verify-changed-files
        run: |
          # Cek status git untuk melihat apakah ada file berubah
          if [ -n "$(git status --porcelain preprocessing/stroke_preprocessed.csv)" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "Perubahan terdeteksi pada dataset."
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "Tidak ada perubahan pada dataset."
          fi
      
      - name: Commit processed data
        if: steps.verify-changed-files.outputs.changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # Kita add spesifik file outputnya
          git add preprocessing/stroke_preprocessed.csv
          git commit -m "Auto: Update dataset preprocessed [skip ci]"
          git push
      
      - name: Upload processed dataset as artifact
        uses: actions/upload-artifact@v4
        with:
          name: preprocessed-dataset
          path: preprocessing/stroke_preprocessed.csv
          retention-days: 30